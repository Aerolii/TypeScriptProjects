const userName: string = 'Jack'
const hasLoggedIn: boolean = true

const num1: number = 1

const reg: RegExp = /foo/

const names: string[] = userName.split('')

const arr: number[] = [1]

const person = {
  first: 'Jack',
  last: 'Herrington'
}

interface Person {
  first: string
  last: string
}

const newPerson: Person = {
  first: 'Jhon',
  last: 'Smith'
}

if (newPerson.first === '3') {
}
